version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - ireturn
    - tagliatelle
    - errorlint
    - bodyclose
    - gosec
    - prealloc

  settings:
    tagliatelle:
      case:
        rules:
          json: snake